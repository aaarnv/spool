node_modules
web
spool-test
output
undefined
.git
.claude
tasks
**/*.webm
**/*.mp4
**/final.mp4
.DS_Store

# Renders draw the brand ground, so the image needs no footage at all. The pool is
# hundreds of MB and stays out; the fallback clip stays in so SPOOL_GROUND=footage
# still renders, on that one clip, without a rebuild.
docs/video/comp/ambient
!docs/video/comp/ambient.mp4
