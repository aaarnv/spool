node_modules
web
spool-test
output
undefined
.git
.claude
tasks
**/*.webm
**/*.mp4
**/final.mp4
.DS_Store

# The ambient pool is gitignored and hundreds of MB; the committed fallback clip is
# the one mp4 the packet renderer actually needs in the image.
docs/video/comp/ambient
!docs/video/comp/ambient.mp4
