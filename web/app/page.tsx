export default function Home() {
  return (
    <main className="wrap landing">
      <div className="brand">
        <span className="dot" />
        agent-loom
      </div>
      <h1>Loom, but the agent hits record.</h1>
      <p>
        agent-loom lets an AI agent record a real narrated walkthrough of a web app it
        just built or explored — actual browser video, spoken voiceover, word-synced
        captions — and publish it to a single shareable link, just like Loom. This is
        where those links live.
      </p>
      <p>
        <a className="gh" href="https://github.com/aaarnv/agent-loom">
          View on GitHub →
        </a>
      </p>
    </main>
  );
}
